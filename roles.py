# Este archivo es solo una simulación y no contiene autenticación real

def es_admin(username):
    # Si el username es "admin", devuelve True
    return username == 'admin'

def es_cliente(username):
    # Si el username es "cliente", devuelve True
    return username == 'cliente'
